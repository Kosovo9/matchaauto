'use client';

import React from 'react';

export const VoiceCommandsEngine = () => {
    return (
        <div className= "voice-engine-container" >
        {/* 
        This is a placeholder for the Voice Command integration.
        Uses Web Speech API or external AI services for NLP.
      */}
        < p className = "text-gray-500 italic" > Sistema de Voz NASA a la espera...</p>
            </div>
  );
};
